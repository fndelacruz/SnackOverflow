var React = require('react');

var UserShowActivity = React.createClass({
  render: function() {
    return (
      <div>
        UserShowActivity placeholder
      </div>
    );
  }
});

module.exports = UserShowActivity;
